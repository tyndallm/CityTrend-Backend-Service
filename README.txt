CityTrend Back-End Service

This is a Java application that is run automatically run through a scheduled Heroku task.  It demonstrates the implementation of MongoDb and Morphia to access existing information as well as utilizes Social media API's from Facebook and Foursquare to aquire new data.